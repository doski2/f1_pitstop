[mypy]
# Skip UI modules and adapter scripts that are dynamic and produce many false positives
# This keeps type checks focused on library code where types are most valuable.
exclude = ^(dashboard.py$|app\\/dashboard.py$|adapters\\/).*\n
# Common helpful options
ignore_missing_imports = True
strict = False
show_error_codes = True
